playwright/openc3-cosmos-pw-test/
*.o
*.def
*.so
*.bundle
*.gem
Makefile
*Gemfile.lock
package-lock.json
.yardoc
.DS_Store
desktop.ini
cacert.pem
.idea
.rubocop-http*
.nyc_output
openc3/*_exception.txt
openc3/*_unexpected.txt
openc3/openc3/
openc3/spec/install/outputs
openc3/spec/examples.txt
coverage/
profile/
plugins/DEFAULT/
**/python/prof/
*.dSYM/
# CosmosUpdateCmdTlm.py input (its generated targets are ignored in
# openc3-cosmos-warplink/.gitignore)
cmd_tlm.json
graph_tree.json
# CFDP transfers (bind mounted into the containers, see compose.yaml)
/cfdp/*
# ...but ship the empty directory, so users need not create it
!/cfdp/.gitkeep

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
pnpm-debug.log*

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore Byebug command history file.
.byebug_history

# Ignore node modules
node_modules

typescript

developer.code-workspace
__pycache__

playwright/tests/enterprise
.vscode/settings.json
